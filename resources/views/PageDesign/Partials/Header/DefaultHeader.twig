{% import "Ceres::PageDesign.Macros.LayoutContainer" as LayoutContainer %}

<header id="page-header" class="fixed-top default-header d-print-none">
	<div class="container-max">
		<div class="row">
			<div id="top-bar" class="col-12 d-flex justify-content-between">

				<div>
					<div id="adv-inner" class="d-none d-lg-block">
						<div class="adv-item rating position-relative h-100 d-inline-block float-left mr-4">
							<span class="position-relative">4,7 / 5,0 Sterne</span>
						</div>
						<div class="adv-item check position-relative h-100 d-inline-block float-left mr-4">
							<span class="position-relative">Expressversand möglich</span>
						</div>
						<div class="adv-item check position-relative h-100 d-inline-block float-left">
							<span class="position-relative">Versandzeit 1-2 Werktage</span>
						</div>
					</div>
				</div>
				<ul class="header-list d-none d-md-none d-lg-flex list-unstyled mb-0">
					<li>
						<a v-if="$store.state.user.userData" href="/my-account" v-cloak>
							Hallo, ${ $store.state.user.userData.firstName }
						</a>
						{#
						<a v-else href="/my-account"> 
						    Mein Konto
						</a>
						<ul class="sub-menu list-unstyled">
						    <li><span class="h4 d-block text-dark font-weight-bold mb-4"><svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#111314" stroke-width="2" stroke-linecap="square" stroke-linejoin="round"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path><circle cx="9" cy="7" r="4"></circle><path d="M23 21v-2a4 4 0 0 0-3-3.87"></path><path d="M16 3.13a4 4 0 0 1 0 7.75"></path></svg> Mein Konto</span></li>
						    <li class="text-dark">
						        <div v-if="$store.state.user.userData" v-cloak>
						            <div class="d-flex justify-content-between text-left mb-3">
						                <span v-if="$store.state.user.userData.id > 0">Kundennummer: ${ $store.state.user.userData.id }</span>
						                <span v-else>${ $store.state.user.userData.email } (Gast)</span>
						            </div>
						            <nav class="nav flex-column mb-3" v-if="$store.state.user.userData.classId > 0">
						                <a class="nav-link text-dark px-0 py-2" href="/my-account?personaldata">Persönliche Daten</a>
						                <a class="nav-link text-dark px-0 py-2" href="/my-account?settings">Kontoeinstellungen</a>
						                <a class="nav-link text-dark px-0 py-2" href="/my-account?history">Auftragshistorie</a>
						            </nav>
						            <button class="btn btn-danger btn-block" v-logout>Ausloggen</button>
						        </div>
						        <login v-else></login>
						    </li>
						</ul>
						#}
					</li>
					{#
					<li>
						<a href="#" class="text-uppercase button-topbar">
							<span>Secura</span>
						</a>
						<ul class="sub-menu list-unstyled">
							<li>
								<span class="h4 d-block text-dark font-weight-bold mb-3 text-nowrap">
									<svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewbox="0 0 24 24" fill="none" stroke="#111314" stroke-width="2" stroke-linecap="square" stroke-linejoin="round">
										<circle cx="12" cy="12" r="10"></circle>
										<line x1="12" y1="16" x2="12" y2="12"></line>
										<line x1="12" y1="8" x2="12.01" y2="8"></line>
									</svg>
									Platzhalter</span>
							</li>
							<li>
								<a {% if services.template.isCheckout() %} target="_blank" {% endif %} href="{{ services.category.getURLById(123) }}">Platzhalter</a>
							</li>
						</ul>
					</li>
				    #}
				</ul>
			</div>
		</div>
		<div class="row flex-row-reverse position-relative">
			<div id="page-header-parent" class="col-12 header-container" data-header-offset>
				{% include "maversandVuePlugin::PageDesign.Partials.Header.HeaderInner" %}
			</div>
		</div>
	</div>
	{% if not services.template.isCheckout() %}
		{% include "maversandVuePlugin::PageDesign.Partials.Header.Navigation" %}
	{% endif %}
	{% cacheslot breadcrumbs %}
	{% if not services.template.isHome() and not services.template.isMyAccount() and not services.template.isCheckout() %}
		{% include "Ceres::PageDesign.Partials.Header.Breadcrumb" %}
	{% endif %}
	{% endcacheslot %}
</header>

<div id="menu-backdrop" class="d-none"></div>
